{% extends "faq/base.html" %}

{% block title %}{{ block.super }}: {{ topic }}{% endblock %}

{% block body %}
<h1 id="section">FAQ:{{ topic }}</h1>
<dl>
{% for question in questions %}
  <dt>{{ question.text }}</dt>
  <dd class="answer">{{ question.answer }}</dd>
{% endfor %}
</dl>

<a href="{% url faq_topic_list %}">Back to the list</a> 
{% endblock %}
